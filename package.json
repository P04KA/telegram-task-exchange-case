{
  "name": "telegram-task-exchange-mvp",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace @task-exchange/api",
    "dev:mini": "npm run dev --workspace @task-exchange/mini-app",
    "dev:admin": "npm run dev --workspace @task-exchange/admin",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces --if-present",
    "seed": "npm run seed --workspace @task-exchange/api"
  }
}
